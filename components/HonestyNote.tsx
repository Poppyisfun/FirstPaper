export default function HonestyNote() {
  return (
    <div className="rounded-2xl border border-dashed border-amber-line bg-amber-soft px-4 py-3 text-sm text-ink-soft sm:px-5 sm:py-4">
      <span className="font-semibold text-amber">
        FirstPaper can be wrong — that&apos;s the point.
      </span>{" "}
      When you use a real paper, treat every explanation as a starting move:
      open the original and check it yourself.
    </div>
  );
}
